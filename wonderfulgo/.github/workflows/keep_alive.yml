name: Keep Render App Awake
on:
  schedule:
    - cron: '*/10 * * * *' # 10分ごとに実行
  workflow_dispatch: # 手動でテスト実行できるようにする設定

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://wonderfulgo.onrender.com > /dev/null
